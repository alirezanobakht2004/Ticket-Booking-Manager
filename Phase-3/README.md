# Ticket Booking Backend (Flask)

## Setup

1. Install requirements: `pip install -r requirements.txt`
2. Create `.env` file with DB and Redis settings.
3. Run: `python app.py`

## Folder Structure
...
